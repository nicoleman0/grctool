ssh:
  PermitRootLogin: "no"
  PasswordAuthentication: "no"

password_policy:
  min_length: 12
  max_age: 90

firewall:
  required: true

system_config:
  auditd_enabled: true
  passwd_permissions: "644"
  shadow_permissions: "640"
  require_updates: true